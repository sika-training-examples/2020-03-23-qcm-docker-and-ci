version: "3.8"
services:
  hello-nginx:
    networks:
      - default
      - traefik
    labels:
      - traefik.frontend.rule=Host:${HOST?}
      - traefik.enable=true

networks:
  traefik:
    external:
      name: traefik
